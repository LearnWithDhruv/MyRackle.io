import React from 'react';

const DescribeButton = ({ onClick }) => {
    return (
        <button onClick={onClick}>
            Describe Testing Instructions
        </button>
    );
};

export default DescribeButton;
